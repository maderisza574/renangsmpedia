import React, { useState, useEffect } from 'react';
import {
  StatusBar,
  StyleSheet,
  Text,
  TextInput,
  TouchableOpacity,
  View,
  Image,
  Dimensions,
  Switch,
} from 'react-native';

export default function Education(props) {
    return(
        <>
        <View>
            <Text style={styles.textaja}>Ini Home</Text>
        </View>
        </>
    )
}

const styles = StyleSheet.create({
  textaja: {
    color: 'black'
  }
});